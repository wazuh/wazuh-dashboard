metadata:
  DEFAULT_SUFFIX: rpmnew
defined_by_user: |-
  server.host: 0.0.0.0

defaults: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

expected: |-
  server.host: 0.0.0.0
  uiSettings:
    overrides:
      "home:useNewHomePage": true

