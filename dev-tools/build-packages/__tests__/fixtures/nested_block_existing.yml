metadata:
  DEFAULT_SUFFIX: dpkg-dist
  EXPECT_PACKAGED_REMOVED: 0
defined_by_user: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

defaults: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

expected: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

