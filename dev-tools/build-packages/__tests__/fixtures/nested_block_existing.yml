metadata:
  DEFAULT_SUFFIX: dpkg-dist
defined_by_user: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

defaults: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

expected: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

