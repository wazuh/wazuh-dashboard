metadata:
  DEFAULT_SUFFIX: rpmnew
  EXPECT_PACKAGED_REMOVED: 0
defined_by_user: |-
  uiSettings:
    overrides:
      "some:otherFlag": false

defaults: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

expected: |-
  uiSettings:
    overrides:
      "some:otherFlag": false
      "home:useNewHomePage": true

