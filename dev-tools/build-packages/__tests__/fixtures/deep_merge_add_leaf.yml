metadata:
  DEFAULT_SUFFIX: rpmnew
defined_by_user: |-
  uiSettings:
    overrides:
      "some:otherFlag": false

defaults: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

expected: |-
  uiSettings:
    overrides:
      "some:otherFlag": false
      "home:useNewHomePage": true

