metadata:
  DEFAULT_SUFFIX: dpkg-dist
defined_by_user: |-
  uiSettings:
    # user has no overrides yet

defaults: |-
  uiSettings:
    overrides:
      "home:useNewHomePage": true

expected: |-
  uiSettings:
    # user has no overrides yet
    overrides:
      "home:useNewHomePage": true

